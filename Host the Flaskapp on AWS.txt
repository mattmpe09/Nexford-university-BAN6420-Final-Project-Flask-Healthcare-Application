Step one
Flask
gunicorn

Step 2
from flask import Flask, render_template

app = Flask(__name__)

@app.route("/")
def home():
    return render_template("index.html")

if __name__ == "__main__":
    app.run()


Step 3
Launch an EC2 Instance
Go to the AWS EC2 Console

Click Launch Instance

Choose an Amazon Linux 2 or Ubuntu AMI

Select a t2.micro (free tier eligible)

Configure security group:

Allow SSH (port 22) from your IP

Allow HTTP (port 80) from anywhere


#Connect to Your EC2 Instance

$ssh -i flask-key.pem ubuntu@your-ec2-public-ip

#Set Up Python Environment
sudo apt update
sudo apt install python3-pip python3-venv -y

#
scp -i flask-key.pem -r FlaskApp/ ubuntu@your-ec2-public-ip:~
cd FlaskApp
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

gunicorn -w 4 -b 0.0.0.0:80 Flaskapp:app



